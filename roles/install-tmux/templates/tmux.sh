export TMUX_VERSION="{{ tmux_version }}"
export TMUX_SOURCE_ROOT="{{ tmux_source_root }}"
export TMUX_BUILD_ROOT="{{ tmux_build_root }}"
export TMUX_INSTALL_ROOT="{{ tmux_install_root }}"

export TPM_VERSION="{{ tpm_version }}"
export TPM_ROOT="{{ tpm_root }}"
